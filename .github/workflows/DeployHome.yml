name: Deploy Home

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  deploy:
    runs-on: self-hosted
    environment: home
    
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      - name: export vars
        run: | 
          export IDLE_COUNT_FILE=${{ vars.IDLE_COUNT_FILE }} > ${{ vars.ENV_PATH }}
          export INHIBIT_URL=${{ vars.INHIBIT_URL }} > ${{ vars.ENV_PATH }}
          export PING_DOMAIN=${{ vars.PING_DOMAIN }} > ${{ vars.ENV_PATH }}
          export PLEX_URL=${{ vars.PLEX_URL }} > ${{ vars.ENV_PATH }}
      - name: Checkout
        uses: actions/checkout@v2
        with:
          path: /home/kewko/standby-monitor

